apiVersion: v1
kind: Secret
metadata:
  name: user-mgmt-secrets
  namespace: user-management
type: Opaque
stringData:
  # Note: In production, use external secrets management (e.g., AWS Secrets Manager, Vault)
  # These are placeholders and should be replaced with actual secrets
  DATABASE_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"
  DATABASE_USER: "dbuser"
  SECRET_KEY: "REPLACE_WITH_ACTUAL_SECRET_KEY"
  REDIS_PASSWORD: ""
