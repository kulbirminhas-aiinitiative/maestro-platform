[tool.poetry]
name = "maestro-audit-logger"
version = "1.0.0"
description = "Maestro Platform - Comprehensive Audit Logging System with Multiple Export Formats"
authors = ["Maestro Platform Team <platform@example.com>"]
readme = "README.md"
packages = [{include = "maestro_audit_logger"}]

[tool.poetry.dependencies]
python = "^3.11"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.0"
pytest-asyncio = "^0.21.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
